# LiteLLM (TritonAI) — required from Step 2. Used by /api/chat.
LITELLM_API_KEY=
# Optional override; defaults to https://tritonai-api.ucsd.edu
LITELLM_BASE_URL=

# Admin gate for editing routes — added in Step 3
ADMIN_PASSWORD=

# Postgres — added in Step 3
DATABASE_URL=

# Public base URL for share links — added in Step 3
NFI_BASE_URL=http://localhost:3000
