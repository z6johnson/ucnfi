import { ComingSoon } from "@/components/ComingSoon";

export default function MemosPage() {
  return (
    <ComingSoon section="Memos" step="Step 4">
      Short, share-ready committee memos drafted from chat sessions or
      directly from a slice of the baseline. Each memo will be editable,
      versioned, and publishable via an unguessable share link.
    </ComingSoon>
  );
}
