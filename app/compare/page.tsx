import { ComingSoon } from "@/components/ComingSoon";

export default function ComparePage() {
  return (
    <ComingSoon section="Compare" step="Step 4">
      A cross-entity comparison matrix — pick any set of entities and
      dimensions and render a side-by-side grid with citations.
    </ComingSoon>
  );
}
